#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main() {
    // ifstream inputFile;

    // inputFile.open("input.txt"); // Open input file

    // // Check if the file opened successfully
    // if (!inputFile) {
    //     cerr << "Error opening input file!" << endl;
    //     return 1; // Return with an error code
    // }

    // // Variables to store the data
    // int c1, c2;

    // inputFile >> c1 >> c2 ;
    // cout << "Values read from file: " << c1 << ", " << c2 << ", " << endl;
    
    // string line;
    // // Read from the input file line by line
    // while (getline(inputFile, line)) {
    //     cout << line << endl; // Print each line to the console
    // }

    // // Close the input file
    // inputFile.close();


// ----------------------------------------------------------------------------

// Input files

    // char name[50];
    // char sal[10];
    // char dept[30];

    // ifstream file;

    // file.open("input.txt");
    
    // char filename[] = "file";

    // if (!file) {
    //     cout << "Cant open input file named " << filename << endl;
    //     exit(1);
    // }

    // while (!file.eof()) {
    //     file >> name >> sal >> dept;
    //     cout << name << "\t" << sal << "\t" << dept << endl;
    // }

    // file.close();


// ----------------------------------------------------------------------------

// Output files

    char name[50];
    char sal[10];
    char dept[30];

    ofstream file2("input2.txt" , ios::out);

    
    

    file2;
    


    
    














    return 0;
}
