#include <iostream>

using namespace std;

/**
 * Print one line for each token in S to draw the cookie.
 *
 * S: cookie name
 */
void solve(string S) {
    // YOUR CODE HERE
}

int main() {
    int T;
    cin >> T;
    for (int i = 0; i < T; i++) {
        string S;
        cin >> S;
        solve(S);
    }
}
