#include <iostream>

using namespace std;

/**
 * Return the minimum number of sets of CALICO blocks needed to
 * form S, or -1 if it is impossible.
 *
 * S: a string
 */
int solve(string S) {
    // YOUR CODE HERE
    return 1;
}

int main() {
    int T;
    cin >> T;
    for (int i = 0; i < T; i++) {
        string S;
        cin >> S;
        cout << solve(S) << '\n';
    }
}
