#include <bits/stdc++.h>
using namespace std;

bool solve(int n, int a, int b) {
    
}

int main() {
    int t;
    cin >> t;
    
    while (t--) {
        int n, a, b;
        cin >> n >> a >> b;
        cout << (solve(n, a, b) ? "YES" : "NO") << "\n";
    }
    
    return 0;
}