#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n;
    cin >> n;
    string S;
    cin >> S;
    
    cout << S << endl;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int T;
    cin >> T;
    while (T--) {
        solve();
    }

    return 0;
}
